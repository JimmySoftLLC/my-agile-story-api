<% include partials/header %>

<h1>The post page</h1>
<p>
   Dude whats going on!
    <% posts.forEach(function(post) { %>
    <li> <%= post.title %> </li>
    <%   }); %>
</p>
<% include partials/footer %>
